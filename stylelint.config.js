/** @type {import('stylelint').Config} */
module.exports = {
  extends: 'stylelint-config-tailwindcss',
  rules: {
    // Aquí puedes añadir o sobreescribir reglas específicas si lo necesitas.
  },
};